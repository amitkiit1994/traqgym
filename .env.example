# Sample environment variables for TraqGym.
# Copy to .env.local for development, or set in the deployment environment.

# Required for QR self-check-in (PR 14). HMAC-SHA256 secret used to sign
# lobby QR tokens. Generate with: openssl rand -hex 32
# WARNING: rotating this invalidates all printed lobby posters.
QR_TOKEN_SECRET=
