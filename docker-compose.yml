services:
  postgres:
    image: postgres:16
    container_name: freeformfitness-postgres
    environment:
      POSTGRES_DB: freeformfitness
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
